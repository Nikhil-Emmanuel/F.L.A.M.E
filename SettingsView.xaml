<UserControl x:Class="F.L.A.M.E.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" Background="GhostWhite"
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid>
        <Grid VerticalAlignment="Center" HorizontalAlignment="Center" Width="410" Height="357">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Number of Guns -->
            <StackPanel Orientation="Horizontal" Margin="0,10,0,10" Grid.Row="0" HorizontalAlignment="Left" Grid.ColumnSpan="2" Width="225">
                <TextBlock Text="Number of Guns     :" FontWeight="Bold" FontSize="14" VerticalAlignment="Center" Width="135"/>
                <TextBox x:Name="GunCountTextBox" Width="70" Margin="20,0,0,0"/>
            </StackPanel>

            <!-- PLC IP Address -->
            <StackPanel Orientation="Horizontal" Margin="0,10,0,10" Grid.Row="1" HorizontalAlignment="Left" Grid.ColumnSpan="2" Width="305">
                <TextBlock Text="PLC IP Address        :" FontWeight="Bold" FontSize="14" VerticalAlignment="Center" Width="135"/>
                <TextBox x:Name="IpAddressTextBox" Width="150" Margin="20,0,0,0" TextChanged="IpAddressTextBox_TextChanged"/>
            </StackPanel>

            <!-- Save Button -->
            <StackPanel Orientation="Horizontal" Margin="130,80,0,0" Grid.Row="2" HorizontalAlignment="Left" Grid.ColumnSpan="2" Width="150">
                <Button Content="Save Settings" Width="150" FontWeight="Bold" FontSize="14" Height="40" Background="LightGreen" Click="SaveSettings_Click" BorderBrush="#FF231F1F">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="10"
                                Padding="{TemplateBinding Padding}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <!-- Hover effect -->
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="border" Property="Background" Value="LightSeaGreen"/>
                                </Trigger>
                                <!-- Clicked effect -->
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="border" Property="Background" Value="DarkGreen"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>

                    </Button.Template>
                </Button>

            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
